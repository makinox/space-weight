import { css } from '@stitches/core';

export const PlanetStars = css({ position: 'absolute', zIndex: '-2', top: '0%', height: '100%', width: '100%' });

export const PlanetCanvas = css({ height: '100% !important', position: 'absolute !important', top: '0%', zIndex: '-1' });
